-- Своя аватарка, отдельно от гугловой.
--
-- Отдельная колонка, а не перезапись avatar_url, по простой причине: при
-- каждом входе через Google мы обновляем имя и картинку из профиля, и своя
-- затиралась бы при следующем же входе. Теперь Google хозяйничает в avatar_url
-- как раньше, а выбор человека живёт здесь и всегда важнее.
ALTER TABLE users ADD COLUMN avatar_own TEXT;
