/**
 * Подставляется при сборке (см. `define` в vite.config.ts): короткий коммит,
 * а там, где git недоступен, — время сборки. Уходит в сообщения о поломках,
 * чтобы отличать «уже починили» от «человек сидит на вчерашней версии».
 */
declare const __BADYUM_BUILD__: string;
