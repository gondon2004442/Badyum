<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
          Сервис, который не даёт системе отобрать микрофон у свёрнутого
          приложения. Наружу не выставлен: запускает его только сам Badyum.
        -->
        <service
            android:name=".VoiceService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Микрофон. Спрашивает его WebView при первом входе в канал: Android
      перекладывает решение на приложение, а Capacitor отвечает согласием
      только если право есть у самого приложения.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      Фоновый сервис разговора. С четырнадцатой версии Android мало объявить
      сервис типа microphone — нужно ещё и одноимённое разрешение, иначе он
      не поднимется.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!--
      Уведомление о том, что разговор идёт. С тринадцатой версии его надо
      просить у человека; отказ не мешает сервису работать, только прячет
      плашку из шторки.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Наушники и гарнитуры. Без объявления Android не покажет приложение в
      списке тех, кому можно отдать звук, на части оболочек.
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
      Микрофон нужен всегда: приложение без него бессмысленно. Но объявить его
      обязательным нельзя — иначе Android не покажет приложение на планшетах и
      части устройств без встроенного микрофона, где гарнитура всё равно есть.
    -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
